# Base de datos PostgreSQL del Gestor de Agenda.
# Usa la misma imagen (con SSL) que la plantilla oficial de Postgres de Railway.
# Existe como Dockerfile para poder fijar la region del servicio en railway.json.
FROM ghcr.io/railwayapp-templates/postgres-ssl:17
